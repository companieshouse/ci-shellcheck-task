#!/bin/bash

# This script is sourced and these variables are then used within the other
# scripts
# shellcheck disable=SC2034
LOGS_REGEX='log/**'
BACKUPS_REGEX='backups*/**'
TESTS_REGEX='test/**'
HIDDEN_FILE_OR_DIR_REGEX='*/[.]**'
